# 정리 
지역변수는 선언한 위치에서만 사용할 수 있다.
딴데에서 쓰면 오류가 난다.

int -> long -> double

작은 범위에서 큰 범위로는 자동 형변환이 가능하다.

명시적 형변환은 자료형 앞에 자료형을 괄호로 넣는것이다.

또한 변수는 꼭 한정된 범위로 지정해서 사용하는 것이 좋다.
이러지 않으면 쓸데없는 메모리를 사용하게 되어 매우 비효율적이다.

또한 좋은 프로그램은 무한한 자유가 있지 않고 어느정도 제약이 있다.

